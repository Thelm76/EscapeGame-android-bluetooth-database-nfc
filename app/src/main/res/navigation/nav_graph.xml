<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragmentStart">
    <fragment
        android:id="@+id/fragmentStart"
        android:name="fr.mastersid.pic2.escapegame.view.FragmentStart"
        android:label="FragmentStart"
        tools:layout="@layout/start_fragment">
        <action
            android:id="@+id/action_fragmentStart_to_fragmentLoby"
            app:destination="@id/fragmentLoby" />
    </fragment>
    <fragment
        android:id="@+id/fragmentLoby"
        android:name="fr.mastersid.pic2.escapegame.view.FragmentLoby"
        android:label="FragmentLoby"
        tools:layout="@layout/loby_fragment">
        <action
            android:id="@+id/action_fragmentLoby_to_fragmentNfc"
            app:destination="@id/fragmentNfc" />
    </fragment>
    <fragment
        android:id="@+id/fragmentNfc"
        android:name="fr.mastersid.pic2.escapegame.view.FragmentNfc"
        android:label="FragmentNfc"
        tools:layout="@layout/nfc_fragment"/>
</navigation>